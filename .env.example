# TeamPro Taekwondo Score Lite - 環境變數範例
# 複製本檔為 .env（.env 已在 .gitignore 中，絕對不可提交）
#
# ⚠️ 前端只能放 anon key。
# ⚠️ 嚴禁把 SUPABASE_SERVICE_ROLE_KEY 放進任何 VITE_ 開頭的變數或前端程式碼。
#    service role key 會繞過 RLS，一旦進入前端 bundle 等於資料庫完全公開。
#
# 取得方式：
#   1. 到 https://supabase.com 建立一個免費專案（區域選 Southeast Asia (Singapore) 延遲最低）
#   2. 專案建立好後進入 Project Settings → API
#   3. 把 Project URL 貼到 VITE_SUPABASE_URL
#      把 anon public 那一把 key 貼到 VITE_SUPABASE_ANON_KEY
#   4. 不需要建立任何資料表，也不需要設定 RLS：
#      本系統只用 Realtime Broadcast 傳訊息，房間狀態存在主控端裝置上。

VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key

# 未設定以上變數時，系統會自動退回本機模擬模式：
#   單機模式 /solo 完全不受影響，仍可離線使用；
#   多裝置房間只能在「同一台裝置的同一個瀏覽器」的不同分頁之間互通，
#   用另一支手機掃 QR Code 會連不上（畫面上會明確標示「本機模擬」）。
